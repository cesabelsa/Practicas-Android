<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme compatible con Material Components -->
    <style name="Base.Theme.TravelApp" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Personaliza tus colores aquí -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorOnPrimary">@color/colorOnPrimary</item>
        <item name="colorSecondary">@color/colorSecondary</item>
        <item name="colorOnSecondary">@color/colorOnSecondary</item>

        <!-- Colores para la barra de estado y navegación (opcional) -->
        <item name="android:statusBarColor" tools:targetApi="l">@android:color/transparent</item>
        <item name="android:navigationBarColor" tools:targetApi="l">@android:color/black</item>
    </style>

    <!-- Tema final usado por el AndroidManifest -->
    <style name="Theme.TravelApp" parent="Base.Theme.TravelApp" />

</resources>

<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.MyApp" parent="Theme.Material3.Light.NoActionBar">

        <!-- Paleta base -->
        <item name="colorPrimary">@color/brand_primary</item>
        <item name="colorOnPrimary">@color/brand_on_primary</item>
        <item name="colorPrimaryContainer">@color/brand_primary_container</item>

        <item name="android:colorBackground">@color/surface_bg</item>
        <item name="colorSurface">@color/surface_bg</item>
        <item name="colorOnSurface">@color/text_primary</item>

        <!-- Tipografía global -->
        <item name="android:fontFamily">sans-serif</item>
        <item name="fontFamily">sans-serif</item>

        <!-- Defaults -->
        <item name="materialButtonStyle">@style/Button.GreenRounded</item>
        <item name="textInputStyle">@style/TextInputLayout.OutlinedGreen</item>
        <!-- Opción global (si quieres): <item name="editTextStyle">@style/TextInputEditText.Green</item> -->

        <!-- Títulos -->
        <item name="textAppearanceTitleLarge">@style/Title.GreenBold</item>

        <!-- Curvatura general -->
        <item name="shapeAppearanceSmallComponent">@style/ShapeAppearance.App.Small</item>
        <item name="shapeAppearanceMediumComponent">@style/ShapeAppearance.App.Medium</item>
        <item name="shapeAppearanceLargeComponent">@style/ShapeAppearance.App.Large</item>

    </style>

</resources>
